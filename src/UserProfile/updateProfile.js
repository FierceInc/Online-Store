import React from 'react';
import styled from 'styled-components'

const UpdateProfile = () => {
    return (
        <React.Fragment>
            
        </React.Fragment>
    );
}

export default UpdateProfile;
