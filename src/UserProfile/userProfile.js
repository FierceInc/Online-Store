import React from 'react';
import styled from 'styled-components'

const UserProfile = () => {
    return (
        <React.Fragment>
                <h1 style={{color: "white", marginTop: "200px"}}>User Profile</h1>
        </React.Fragment>
    );
}
export default UserProfile;
